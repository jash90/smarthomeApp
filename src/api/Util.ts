export default class Util {
    public static serialize(response: any) {
        //     if (response.error)
        //         throw Error(response.error);
        //    // if (!response.data)
    }
}
